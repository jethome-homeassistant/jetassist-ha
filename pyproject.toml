[tool.ruff]
line-length = 120

[tool.ruff.lint]
ignore = [
    "TC002",   # Move third-party import into TYPE_CHECKING block (not needed in HA)
    "TC003",   # Move stdlib import into TYPE_CHECKING block (not needed in HA)
    "RUF006",  # Store reference to asyncio.create_task (fire-and-forget is idiomatic in HA)
]
